# 模型分级权限系统 - 全量修复开发记录

## 日期
2026-05-13

## 任务概述
实现完整的三级模型权限体系（Lv0/Lv1/Lv2），包含自动编号、分级消耗、向下兼容等。

## 变更内容

### 1. Lv0 公开模型权限（未绑定用户）
**文件**: `commands_user.py`
- `cmd_help()`: 新增身份等级说明（Lv0/Lv1/Lv2）
- `cmd_models()`: 未绑定用户也可查看公开模型列表
- `cmd_switch()`: 未绑定用户可在私聊切换公开模型
- `cmd_status()`: 未绑定用户显示 Lv0 身份信息

### 2. 二级赞助优先消耗 + 分级天数管理
**文件**: `user_manager.py`、`cron_tasks.py`
- 新增 `_migrate_umo_data()`: 自动迁移旧数据到新的分级存储格式
- 新增字段: `l1_days`、`l2_days`、`active_level`
- `bind_user()`: 按等级累加天数，二级绑定后 `active_level` 设为 "2"
- `_cron_daily()`: 优先消耗 `active_level` 天数，耗尽后自动切换至低等级
- `_process_single_order()`: 轮询自动绑定时使用 `_infer_plan_level()` 推断等级

### 3. 高级用户可使用低级模型
**文件**: `user_manager.py`
- `has_model_permission()`: Lv2 用户可使用 Lv2+Lv1+Lv0 模型；Lv1 用户可使用 Lv1+Lv0 模型
- `_get_level_prefixes()`: 统一获取指定等级前缀列表

### 4. 模型自动编号系统
**文件**: `commands_user.py`、`commands_admin.py`
- `_get_all_available_models()`: 为每个模型生成编号（zero_1, one_1, two_1 等）
- `cmd_models()`: 展示编号，标识当前使用模型
- `cmd_switch()`: 支持按编号切换（如 `/afdian_switch zero_1`）
- `cmd_addmodels()`: 添加后显示编号
- `cmd_status()`: 显示分级天数详情（消耗中/暂停）

### 5. 兼容性修复
- 修复 `cmd_bind()` 中 `prefixes` 字符串被按字符迭代的 bug
- 移除 `cmd_switch()` 中群聊/私聊权限拒绝的重复代码
- 新增旧数据自动迁移机制，存量用户数据无需手动处理
- 所有文件通过 `py_compile` 语法检查

## 存储格式变更

旧格式 (umo_data):
```json
{
  "remaining_days": 30,
  "prefixes": "model1,model2",
  "level": "1",
  ...
}
```

新格式 (umo_data):
```json
{
  "remaining_days": 60,
  "l1_days": 30,
  "l2_days": 30,
  "active_level": "2",
  "prefixes": "model1,model2,model3",
  "level": "2",
  ...
}
```

迁移逻辑: 首次访问旧数据时自动补充 `l1_days`/`l2_days`/`active_level` 字段，不影响原有数据。

## 测试要点
- [ ] 未绑定用户执行 `/afdian_models` 应看到公开模型
- [ ] 未绑定用户执行 `/afdian_switch zero_1` 应成功切换
- [ ] 未绑定用户尝试切换非公开模型应被拒绝
- [ ] Lv2 用户执行 `/afdian_status` 应显示二级在消耗、一级暂停
- [ ] 每日零点 Lv2 天数递减，Lv1 不变
- [ ] Lv2 耗尽后自动切换至 Lv1
- [ ] 旧用户数据自动迁移，登录后正常显示
- [ ] `/afdian_addmodels 0 xxx` 响应显示 `zero_N` 编号
